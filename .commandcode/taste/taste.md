# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# discord
- Use Discord role IDs instead of role names for authorization. Two-tier structure: "admin" roles have access to all commands, "user" roles can only self-add to the whitelist. Both tiers support multiple IDs in a list. Confidence: 0.85

